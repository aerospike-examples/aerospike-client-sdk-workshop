This directory contains synthetic product JSON files (~150 products) that are auto-loaded on application startup.

Each file is named `{id}.json` and follows the format expected by `JsonParsingService`. No manual data loading is required.
